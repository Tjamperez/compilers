() | int / g { return 0, }
() | int / f {
     g(),
     return 0,
}

(int a) | int / factorial
{

  int a1; a2; a3; a4; x1; x2; x3,

  x1 = a1 + b1,
  if (a < 1) { return 1, },
  x1 = a1 + b1 * c1 + d1,
  x2 = a2 + b2,
  x3 = a3 + b3,
  return a * factorial(a - 1),
}